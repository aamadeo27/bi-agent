# Permission-block, clarification & message states (S9 + §11)

description: Build the message-state components: PermissionBlockMessage (S9 / Flow 6
— error left-border, shield icon, "Access restricted" heading, plain-language body,
structured `missing[]` resource list with lock icons + monospace identifiers,
suggestion footer), ClarificationMessage (warning variant, streamed body), and the
generic error bubble with "Try again". Also the welcome/empty state with example
prompt chips. All consume the contract shapes from the SSE stream.

tag: frontend
deps: T6.2

acceptance criteria:
- PermissionBlockMessage renders the PermissionBlock `missing[]` exactly per Flow 6;
  no chart/table; input stays active (GAP-12 block+explain).
- Clarification + generic error states match §11; "Try again" re-submits.
- Welcome empty state shows example prompt chips per §11.
- RTL tests for each state; axe passes; color never the sole signal (icon+text).

kb-refs:
  patterns:    [error-handling]
  contracts:   [permission-block, chat-api, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
design-refs: [s9-partial-permission-block, primary-flows, components-chat-workspace, states-empty-loading-error]
