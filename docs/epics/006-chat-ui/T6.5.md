# Query Inspect drawer (S3)

description: Build the S3 Query Inspect drawer: "View query" button rendered only
for roles with `canInspectQuery` (from `/me` capabilities), right-side drawer
(40vw) over the timeline, metadata strip (source, type badge, executedAt, row
count), syntax-highlighted line-numbered read-only code block, copy-to-clipboard,
loading/error states. Wire to `GET /api/messages/:id/query`. Focus-trap + Escape.

tag: frontend
deps: T6.2, T5.7

acceptance criteria:
- "View query" appears only for capability-enabled roles; opens the drawer.
- Drawer shows query + metadata per UI/UX §7/Flow 5; copy works; Escape/X closes.
- Loading + "details unavailable" error states handled.
- Focus management per §10; RTL + axe tests pass.

kb-refs:
  patterns:    [rbac-grant-model, error-handling]
  contracts:   [generated-query-view, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
