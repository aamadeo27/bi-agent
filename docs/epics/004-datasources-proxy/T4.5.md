# Data Sources UI (S7)

description: Build the S7 Admin Data Sources screen: connection list/cards (name,
type badge, status dot, last-tested), add/edit modal (name, type selector for the
v1 set, connection fields with masked credentials + reveal toggle, "Test
connection" with inline result), delete confirm, re-test. Credentials are
write-only; never displayed. Wire to the registry + test endpoints.

tag: frontend
deps: T3.4

acceptance criteria:
- S7 matches UI/UX §7: list, add/edit modal, test result inline, delete confirm.
- Credential fields are masked and never shown after save.
- Type selector covers the v1 set (pg/mysql/bigquery/rest).
- RTL + axe tests pass; modal focus-trap + Escape.

kb-refs:
  patterns:    [error-handling]
  contracts:   [rbac-model, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
design-refs: [s7-admin-data-sources]
