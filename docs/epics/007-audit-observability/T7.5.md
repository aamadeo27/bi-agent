# Dashboard provisioning — DEFERRED (post-v1)

> **STATUS: DEFERRED to post-v1** (user scope decision, 2026-06-14). v1 observability
> is audit persistence (T7.1/T7.2) + OTel/pino instrumentation (T7.3) + the single P1
> gate-bypass alert (T7.4, A1 only). The 6-panel dashboard below is fully specified in
> `monitoring.md §4` and is wired manually from that runbook if needed before this task
> is scheduled. Do NOT implement in v1.

description: Provision the observability dashboard from `monitoring.md §4` as
importable config (dashboard JSON / IaC), with the six panel groups: security gate
(headline gate-bypass single-stat in red when > 0), query-pipeline latency (with the
6 s SLO line), LLM cost/tokens, errors (expected vs failure class), SSE streaming
health, and auth/throughput. Panels reference the metric names + labels defined in
the metrics catalog (§1). Add the SLO recording rule for
`ask.e2e.app.duration.s` (e2e minus data-source time) used by the latency panels and
alert A5.

tag: infra
deps: T7.3

acceptance criteria:
- One dashboard with the six panel groups from monitoring.md §4, each panel bound to
  the catalogued metric + aggregation.
- Gate bypass/error headline tile renders red on any non-zero value.
- Latency panels show p50/p95/p99 with the 6 s SLO threshold line; the
  `ask.e2e.app.duration.s` recording rule (data-source time excluded) exists and is
  used by both the panel and alert A5.
- Dashboard is committed as importable config (not manually built) and is
  backend-target configurable.

kb-refs:
  - docs/kb/patterns/streaming-sse.md
  - docs/kb/patterns/multi-tenant-request-scoping.md
  - docs/kb/contracts/error-codes.md
  - docs/kb/conventions/naming.md
  - docs/kb/conventions/testing.md
  - docs/kb/tech-stack/observability.md
