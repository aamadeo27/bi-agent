# Audit Log UI (S8)

description: Build the S8 Admin Audit Log screen (P1): filter bar (date range, event
type multi-select, user search, data-source filter), paginated event table
(timestamp, user, type, description, data source, status), row-expand/detail panel
(full metadata incl. query text where applicable, role-at-event, IP, outcome),
export-filtered-as-CSV, and empty state. Wire to `GET /api/admin/audit`.

tag: frontend
deps: T7.1, T3.4

acceptance criteria:
- S8 matches UI/UX §7: filters, paginated table, detail panel, CSV export, empty.
- Status uses color + icon + text (never color alone) per §10.
- RTL + axe tests pass.

kb-refs:
  patterns:    [error-handling]
  contracts:   [audit-event, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
