# Login screen (S1) & auth UI states

description: Build S1 Login: centered card, tenant context from subdomain when
resolvable, email+password form, conditional SSO button (shown when the tenant has
OIDC), "Forgot password?" link, and the idle/loading/error states from UI/UX §7.
Wire to the auth API + token storage (access in memory, refresh via cookie). Handle
session-expired redirect with banner.

tag: frontend
deps: T1.4

acceptance criteria:
- S1 renders per spec with proper labels and WCAG 2.1 AA focus/contrast.
- Email+password login works against the API; SSO button initiates the OIDC flow.
- Error and loading states match UI/UX §7/§11; session-expired redirect works.
- RTL + axe tests pass for S1.

kb-refs:
  patterns:    [auth-flow, error-handling]
  contracts:   [auth-api, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
