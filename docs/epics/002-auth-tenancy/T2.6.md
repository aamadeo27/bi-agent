# Account/profile (S10) & /me endpoint

description: Implement `GET /api/me` (returns `{user, role, capabilities, tenant}`)
and the S10 Account/Profile screen: editable display name, read-only email / active
role / tenant, password-change section (only for password-auth users), and
"sign out of all sessions". Also build the S11 error/boundary screens
(404/403/tenant-boundary/session-expired) since they share the auth shell.

tag: frontend
deps: T2.5

acceptance criteria:
- `/me` returns the shape the SPA shell needs (role capabilities included).
- S10 renders and persists display-name + password changes per spec.
- S11 variants render with correct CTAs (UI/UX §7).
- RTL + axe tests pass for S10 and S11.

kb-refs:
  patterns:    [auth-flow, error-handling, multi-tenant-request-scoping]
  contracts:   [auth-api, rbac-model, error-codes]
  conventions: [naming, testing]
  tech-stack:  [react, tailwind]
