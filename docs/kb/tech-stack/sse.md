# Streaming transport — Server-Sent Events (SSE)

**Chosen:** Server-Sent Events (SSE)  
**Alternatives:** WebSocket, HTTP chunked

One-way token stream fits chat; simpler than WS; works through proxies/CDN.
