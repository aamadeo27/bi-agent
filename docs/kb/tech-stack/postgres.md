# Control-plane DB — PostgreSQL 16

**Chosen:** PostgreSQL 16  
**Alternatives:** MySQL, CockroachDB

Schema-per-tenant (`search_path`), strong RBAC/`GRANT` for the restricted-credential model, JSONB for flexible audit.
