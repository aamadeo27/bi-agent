# Auth — Custom email+password (argon2) + OIDC via `openid-client`

**Chosen:** Custom email+password (argon2) + OIDC via `openid-client`  
**Alternatives:** Auth0/Clerk, Lucia, NextAuth

Locked: email+password baseline + optional per-tenant SSO/OIDC; keeps tenant IdP config in-app.
